# PsychPATH Database Configuration
# Copy this to .env to fix database persistence issues

# Database Engine (PostgreSQL)
DB_ENGINE=django.db.backends.postgresql

# Database Configuration
DB_NAME=psychpath
DB_USER=macdemac
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Disable SQLite fallback
USE_SQLITE=0

# Django Settings
DEBUG=1
SECRET_KEY=dev-secret-key-change-in-production

# Security
ALLOWED_HOSTS=localhost,127.0.0.1,DigitalOcean,testserver,backend
